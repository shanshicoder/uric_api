# 根据requirements.txt文件安装

```python
pip install -r requirements.txt
```



# 迁移数据库

```
python manage.py makemigrations（生成迁移文件）
python manage.py migrate（将变更应用到数据库）
python manage.py showmigrations 你的app名称  c
```

